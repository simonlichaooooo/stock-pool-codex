# 股池管理需求文档

## 1. 产品概述

股池管理是一个面向个人投资研究的私人 Web 应用，用于保存股票池、维护估值假设并计算预期回报。系统当前为单页前端应用，后端依赖 Supabase Auth、PostgREST、RLS 策略和数据库表。

## 2. 用户角色

| 角色 | 说明 | 核心权限 |
| --- | --- | --- |
| 未登录用户 | 访问应用但未认证 | 查看登录页，使用 GitHub 或邮箱 Magic Link 登录 |
| 普通用户 | 已登录且完成昵称设置 | 管理自己的私人股池 |

## 3. 模块需求

### 3.1 认证与用户资料模块

**目标**

保证用户身份可信，并为私人数据隔离提供唯一用户资料。

**功能需求**

- 支持 GitHub OAuth 登录。
- 支持邮箱 Magic Link 登录。
- 登录会话需保存到浏览器本地，并在 token 即将过期时自动刷新。
- 首次登录用户必须设置唯一昵称后才能进入主应用。
- 用户资料包含邮箱和唯一昵称。
- 昵称必须唯一，重复时给出明确提示。
- 用户可退出登录，退出后清空本地会话和当前页面状态。

**数据依赖**

- `auth.users`
- `profiles`
- 本地存储键：`stockpool.supabaseSession`

**权限要求**

- 用户只能创建和更新自己的 profile。
- 用户只能读取和更新自己的 profile。

### 3.2 我的股池模块

**目标**

让用户维护自己的股票研究列表，并以统一表格展示关键估值指标。

**功能需求**

- 支持新增、编辑、删除股票。
- 支持按股票名称或代码搜索股池记录。
- 支持按市场筛选：全部、A 股、H 股、美股。
- 支持股票置顶、置底和手动顺序排序。
- 表格需展示：
  - 股票名称与代码
  - 最新市值
  - 30 周乖离率及过去 2 年分位值（支持分别排序）
  - upside
  - Forward PE
  - 前瞻股东回报率
  - 前瞻市值
  - 前瞻利润
  - 前瞻 PE
  - 净现金
  - 现金折扣
  - 前瞻股东回报
- 支持按 upside、Forward PE、前瞻股东回报率排序。
- 股池需按 30 周乖离率、2 年分位和最近 3 周乖离趋势展示弱周期极端、强周期极端、叙事展开与叙事压缩提醒。
- H 股股东回报率支持切换“20% 股息税后”口径。

**数据依赖**

- `stock_records.payload`
- `stock_records`

**业务规则**

- 用户只能读写自己的私有股票记录。
- 删除股票前需要确认。

### 3.3 股票匹配与行情模块

**目标**

降低录入成本，自动匹配股票基础信息并获取最新市值。

**功能需求**

- 在新增/编辑抽屉中输入股票名称或代码时触发搜索。
- 优先使用东方财富搜索接口匹配 A 股、H 股、美股。
- 搜索失败或无结果时，使用内置备用样例。
- 选择股票后自动填充：
  - 名称
  - 代码
  - 市场
  - 交易所
  - 行情标识
  - 默认展示货币
  - 最新市值
  - 市值更新时间
- 市值优先使用腾讯行情接口，失败后使用东方财富行情接口，再失败则使用内置备用市值。
- 支持人民币、港币、美元展示和换算。
- 汇率优先从 `open.er-api.com` 获取，失败后使用固定备用汇率。

**业务规则**

- 所有估值计算统一以 CNY 亿为内部基准。
- 用户可切换展示货币，但保存时金额字段仍需转换为 CNY。

### 3.4 估值测算模块

**目标**

基于用户输入的前瞻利润、PE、净现金和股东回报假设，实时计算估值结果。

**输入字段**

- 前瞻利润
- 前瞻 PE
- 前瞻年份
- 利润备注
- 前瞻股东回报
- 股东回报政策
- 净现金
- 净现金口径
- 净现金打折系数
- 我的备注

**计算规则**

- 前瞻市值 = 前瞻利润 × 前瞻 PE + 净现金 × 净现金打折系数
- upside = 前瞻市值 / 最新市值 - 1
- Forward PE = (最新市值 - 净现金 × 净现金打折系数) / 前瞻利润
- 前瞻股东回报率 = 前瞻股东回报 / 最新市值
- H 股税后口径下，前瞻股东回报率 × 0.8。

**功能需求**

- 抽屉右侧实时展示测算结果。
- 输入金额字段时需按当前展示货币进行显示，并按汇率转为 CNY 保存。
- 净现金打折系数应限制在 0 到 1 的合理范围。
- 对无效或缺失数据展示 `--`，避免错误计算污染界面。

### 3.5 数据安全与权限模块

**目标**

通过 Supabase RLS 保护多用户数据边界。

**功能需求**

- `stock_records` 开启 RLS。
- 普通用户只能读写自己的股票记录。
- `profiles` 开启 RLS。
- 用户只能读写自己的 profile。

### 3.6 界面与响应式模块

**目标**

在桌面和移动端都能高效管理股池。

**功能需求**

- 顶部固定导航展示产品名、用户邮箱、昵称和退出按钮。
- 主导航包含股池、组合管理、市场洞察和日志。
- 桌面端使用宽表格展示股池。
- 移动端表格自动转为卡片式布局。
- 新增/编辑股票使用右侧抽屉。
- 抽屉内采用左侧表单、右侧结果面板布局，移动端自动单列。
- 空状态需给出明确下一步引导。
- 重要失败场景需给出可理解的错误提示。

## 4. 核心数据模型

### 4.1 stock_records

保存用户股票记录。`payload jsonb` 保存完整业务对象，结构化列用于归属和时间查询。

关键字段：

- `id`
- `user_id`
- `payload`
- `created_at`
- `updated_at`

### 4.2 profiles

保存用户的私人账户资料。

关键字段：

- `id`
- `email`
- `nickname`
- `created_at`
- `updated_at`

## 5. 关键业务流程

### 5.1 新用户进入系统

1. 用户通过 GitHub 或邮箱 Magic Link 登录。
2. 系统解析并保存 Supabase session。
3. 系统查询 profile。
4. 若 profile 不存在，要求设置唯一昵称。
5. 昵称创建成功后加载用户自己的股池及其他私人数据。

### 5.2 新增股票并测算

1. 用户点击“新增股票”。
2. 在抽屉中搜索股票。
3. 系统匹配股票并获取市值。
4. 用户填写前瞻利润、PE、净现金、股东回报等假设。
5. 系统实时计算前瞻市值、upside、Forward PE 和股东回报率。
6. 用户保存，系统写入 `stock_records`。

## 6. 非功能需求

- **可用性**：核心操作失败时需展示明确错误，不能静默失败。
- **安全性**：所有多用户数据访问必须依赖 RLS，前端判断只用于体验优化。
- **兼容性**：移动端需保持股池表格可读可操作。
- **容错性**：行情、汇率接口不可用时，应使用备用数据或固定汇率。
- **数据一致性**：股池保存需保持 `payload` 与更新时间一致。

## 7. 后续可补充需求

- 增加用户资料编辑入口，例如修改昵称。
- 增加更多市场、ETF、指数或加密资产类型。
- 增加导入导出能力，例如 CSV 或 Excel。
- 增加更完整的测试用例和部署文档。
- 将单文件前端拆分为模块化工程，便于长期维护。
